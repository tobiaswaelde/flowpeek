meta {
  name: get personal preferences
  type: http
  seq: 1
}

get {
  url: {{host}}/api/v1/settings/preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
