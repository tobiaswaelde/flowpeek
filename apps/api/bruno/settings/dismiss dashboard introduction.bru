meta {
  name: dismiss dashboard introduction
  type: http
  seq: 2
}

put {
  url: {{host}}/api/v1/settings/preferences/intro-banners/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
