meta {
  name: restore page introductions
  type: http
  seq: 3
}

delete {
  url: {{host}}/api/v1/settings/preferences/intro-banners
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
